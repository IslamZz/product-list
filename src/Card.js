import React from 'react';
import Button from "./Button";

function Card(props) {
    return (
        <div className='card-button'>
            <Button />
        </div>
    );
}

export default Card;